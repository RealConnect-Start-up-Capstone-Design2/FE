/**
 * @typedef {object} ButtonProps
 * @property {string} label - The text to display on the button.
 * @property {() => void} onClick - The function to call when the button is clicked.
 * @property {boolean} [disabled] - Whether the button is disabled.
 * @property {'primary' | 'secondary'} [variant] - The style variant of the button.
 * @property {boolean} [fullWidth] - Whether the button should take up the full width.
 */
export const _ = {}; 